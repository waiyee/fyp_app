<% if @post.errors.any? %>
  $("#flash_notice").hide(300);
  $("#song_errors").html("<%= escape_javascript(render 'shared/error_messages', :object => @song) %>");
  $("#song_errors").show(300);
<% end %>